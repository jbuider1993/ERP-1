import React from 'react';
import MonitorShow from '../../components/resource/monitorShow';

const MonitorPage = (props) => {

  const monitorShowProps = {};

  return (
    <div>
      <MonitorShow {...monitorShowProps} />
    </div>
  );
};

export default (MonitorPage);
